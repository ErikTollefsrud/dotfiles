# vim: filetype=zsh

echo "DEBUG: \$TERM_PROGRAM is currently set to: '$TERM_PROGRAM'"
if [[ "$TERM_PROGRAM" == "vscode" ]]; then
    source ~/dotfiles/zsh/dot-vscode-zshrc
fi

if [[ "$TERM_PROGRAM" == "ghostty" ]]; then
    source ~/dotfiles/zsh/dot-default-zshrc
fi